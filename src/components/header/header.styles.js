import styled from 'styled-components';

const StyledHeader = styled.header`
	position: relative;
	display: flex;
	justify-content: space-between;
	overflow-x: clip;
	padding: 24px;
`;

export { StyledHeader };
